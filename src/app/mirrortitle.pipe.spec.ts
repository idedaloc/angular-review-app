import { MirrortitlePipe } from './mirrortitle.pipe';

describe('MirrortitlePipe', () => {
  it('create an instance', () => {
    const pipe = new MirrortitlePipe();
    expect(pipe).toBeTruthy();
  });
});
